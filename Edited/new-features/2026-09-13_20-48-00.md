# Session: SysInfo Power & Uptime, Theme Sync Fix, Dual Antigravity, Auto-Maximize

**Date:** 2026-09-13 20:48:00
**Category:** new-features (also: fixes, changes)

## Modifications
- Added system uptime display to SysInfo QuickShell popup (bottom left)
- Added CPU package power (PPT wattage) to SysInfo CPU section header
- Added total system power draw to SysInfo popup (bottom right, APU + dGPU on AC, battery discharge on battery)
- Fixed rofi symlink pointing to old Hyprland dotfiles instead of DARK_NIRI
- Replaced 500+ hardcoded Tokyo Night hex colors with dynamic `Components.Theme.*` tokens across all QML components (Settings, SysInfo, Audio, Bluetooth, Brightness, Mic, Network, Screencast)
- Added missing `import "." as Components` to 6 QML component files
- Created dual Antigravity launcher script (`scripts/dual-antigravity.sh`) — opens two IDE windows for the same workspace
- Added dual Antigravity auto-launch to Niri startup (30s delay for second window)
- Added global Niri window rule: all apps open maximized (`proportion 1.0`) by default
- Added Session Edit Logs rule (#3) to AGENTS.md with `Edited/` directory structure

## Files Modified
- `quickshell/sysinfo.sh` — added uptime, cpu_power, total_power fields
- `quickshell/components/SysInfo.qml` — added uptime, cpuPower, totalPower UI
- `quickshell/components/Settings.qml` — 487 hardcoded hex → Theme tokens
- `quickshell/components/Audio.qml` — Theme tokens + import
- `quickshell/components/Bluetooth.qml` — Theme tokens + import
- `quickshell/components/Brightness.qml` — Theme tokens + import
- `quickshell/components/Mic.qml` — Theme tokens + import
- `quickshell/components/Network.qml` — Theme tokens + import
- `quickshell/components/Screencast.qml` — Theme tokens + import
- `scripts/dual-antigravity.sh` — new dual-window launcher
- `niri/startup.sh` — added dual Antigravity auto-launch
- `niri/config.kdl` — added global maximize window rule
- `AGENTS.md` — added Session Edit Logs rule
